from setuptools import setup, find_packages

setup(
    name="mech_interp",
    version="0.1",
    packages=find_packages(),
    description="Mechanistic Interpretability Research On LayerNorm in LLMs",
    python_requires=">=3.8",
) 